# --- Core / env ---
python-dotenv>=1.0.0

# --- LangChain core + orchestration (create_retrieval_chain, ChatPromptTemplate, etc.) ---
langchain-core>=0.3.0
langchain-classic>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# --- Vector store (Chroma Cloud) ---
chromadb>=0.5.0
langchain-chroma>=0.2.0

# --- Embeddings (Google Gemini) ---
langchain-google-genai>=2.0.0

# --- LLM inference (Groq) ---
langchain-groq>=0.2.0

# --- Sparse / hybrid retrieval (BM25) ---
rank_bm25>=0.2.2

# --- Cross-encoder reranking ---
sentence-transformers>=3.0.0

# --- Document loading (PDF, DOCX, images, txt) ---
unstructured[pdf,docx,image]>=0.15.0

# --- Observability / tracing ---
langsmith>=0.1.100

# --- FastAPI service layer (app/backend/router.py) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
slowapi>=0.1.9
python-multipart>=0.0.9
pydantic>=2.7.0

# --- Database (Postgres async, used by Database/relational_db.py & models.py) ---
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# --- Auth (Supabase, used by auth/oauth.py & auth/security.py) ---
supabase>=2.0.0